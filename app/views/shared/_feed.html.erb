<% if @feed_items.any? %>
    <%= will_paginate @feed_items, per_page: 10 %>
  <ol class="microposts">
    <%= render partial: 'shared/feed_item', collection: @feed_items %>
  </ol>
  <%= will_paginate @feed_items, per_page: 10 %>
<% end %>